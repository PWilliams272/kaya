# Copilot Instructions For kaya

Canonical instructions for this repo live in **`CLAUDE.md`** at the repo root. Read that file —
architecture, run/validate commands, deploy path, and constraints are all there.

Cross-repo context (deploy targets, ports, subdomains, IAM, integration modes) lives in
`/Users/peterwilliams/projects/system-overview`. Personal defaults that apply everywhere
(Python 3.11 + ruff + pytest, branch model, AWS profiles, secret handling) live in
`~/.claude/CLAUDE.md`.

Do not add repo guidance to this file — it will drift from `CLAUDE.md`. Edit `CLAUDE.md` instead.
